# Copy to a local shell profile or source it directly:
#   source platform/db/psql.env.example

export PGHOST=localhost
export PGPORT=5432
export PGUSER=postgres
export PGDATABASE=postgres
# Optional:
# export PGPASSWORD='your_password'
